# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $super Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$super to drag floating windows to their wanted position
floating_modifier $super

# focus_follows_mouse no
#mouse_warping none

# start a terminal
bindsym $super+Return $exid gnome-terminal

# kill focused window
bindsym $super+Shift+q kill

# to prevent chrome from closing itself
bindsym $alt+F4 exec --no-startup-id echo

set $exid exec --no-startup-id
set $exaid exec_always --no-startup-id

bindsym $super+d $exid rofi -show drun

# change focus
#bindsym $super+j focus left
#bindsym $super+k focus down
#bindsym $super+l focus up
#bindsym $super+odiaeresis focus right

# alternatively, you can use the cursor keys:
bindsym $super+Left focus left
bindsym $super+Down focus down
bindsym $super+Up focus up
bindsym $super+Right focus right

# move focused window
#bindsym $super+Shift+j move left
#bindsym $super+Shift+k move down
#bindsym $super+Shift+l move up
#bindsym $super+Shift+odiaeresis move right

# alternatively, you can use the cursor keys:
bindsym $super+Shift+Left move left
bindsym $super+Shift+Down move down
bindsym $super+Shift+Up move up
bindsym $super+Shift+Right move right

# split in horizontal orientation
bindsym $super+h split v

# split in vertical orientation
bindsym $super+v split h

# enter fullscreen mode for the focused container
bindsym $super+f fullscreen toggle
# toggle polybar
bindsym $super+Shift+f $exid "sh -c 'if systemctl --user -q is-active polybar@\*; then systemctl --user stop polybar@\*; else systemctl --user start polybar@\* -all; fi'"

# change container layout (stacked, tabbed, toggle split)
bindsym $super+t layout toggle stacking tabbed
bindsym $super+e layout toggle split

# toggle tiling / floating
bindsym $super+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $super+space focus mode_toggle

# focus the parent container
#bindsym $super+a focus parent

# focus the child container
#bindsym $super+d focus child

bindsym $super+Shift+r restart

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         status_command exec $XDG_CONFIG_HOME/i3status/custom.sh
#         tray_output none
#         position bottom
# }
#$exaid $XDG_CONFIG_HOME/polybar/launch.sh
#$exaid $XDG_CONFIG_HOME/glava/launch.sh
#$exaid sh -c "sleep 10; sudo rfkill unblock all"

#bindsym button3 nop
#bindsym button4 nop
#bindsym button5 nop

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindcode 44 resize shrink width 10 px or 10 ppt
        bindcode 45 resize grow height 10 px or 10 ppt
        bindcode 46 resize shrink height 10 px or 10 ppt
        bindcode 47 resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindcode 113 resize shrink width 10 px or 10 ppt
        bindcode 116 resize grow height 10 px or 10 ppt
        bindcode 111 resize shrink height 10 px or 10 ppt
        bindcode 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $super+r
        bindsym Escape mode "default"
        # Enter
        bindcode 36 mode "default"
}

bindsym $super+r mode "resize"

default_border none
for_window [class="jetbrains-toolbox"] floating enable
for_window [class="steam_app.*"] floating enable
for_window [instance="sun-awt-X11-XWindowPeer"] floating enable
for_window [class="Gnome-network-displays"] floating enable
for_window [class="Microsoft Teams" title="Microsoft Teams Notification"] floating enable
no_focus [class="Microsoft Teams" title="Microsoft Teams Notification"]

$exaid $XDG_CONFIG_HOME/i3/rc.sh

set $displayMode "Set display mode [w]ork [l]aptop"
#bindsym $super+x mode $displayMode
mode $displayMode {
    bindsym w exec $XDG_CONFIG_HOME/screenlayouts/work.sh; mode "default"; restart
    bindsym l exec $XDG_CONFIG_HOME/screenlayouts/laptop.sh; mode "default"; restart

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

#bindsym Pause exec suspend
#bindsym Scroll_Lock exec lock

set $Power " [S]leep, [p]oweroff, [l]ogoff or [r]eboot?"
bindsym $super+q mode $Power

mode $Power {
    bindsym s exec systemctl suspend; mode "default"
    bindsym p exec systemctl poweroff
    bindsym r exec systemctl reboot
    bindsym k exec sudo systemctl kexec
    bindsym l exec i3-msg exit
    bindsym Escape mode "default"
}

# Emoji Keyboard

bindsym $super+period $exid sh -c 'splatmoji copypaste'
bindsym $super+k $exid "sh -c 'if setxkbmap -query | grep us; then setxkbmap de; else setxkbmap us; fi'"
bindsym $super+m $exid morse
bindsym $super+p $exid sh -c 'fd --no-ignore-vcs --hidden -t f workspace.xml $HOME -X ls --format=vertical --sort=time --time=ctime | sed -r "s#/\.idea/workspace.xml\$##g" | sed -r "s#$HOME/##g" | grep -v "^\\." | grep -v node_modules | dmenu -i -no-custom -p Project | xargs -i i3-msg "exec systemd-cat --stderr-priority=warning --identifier=intellij intellij-idea-ultimate-edition $HOME/{}"'

# Pulse Audio controls
bindsym XF86AudioRaiseVolume $exid pactl set-sink-volume @DEFAULT_SINK@ +5%; $exid pactl set-sink-mute @DEFAULT_SINK@ 0
bindsym XF86AudioLowerVolume $exid pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioMute $exid pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym Shift+XF86AudioLowerVolume $exid pctl volume 0.05-
bindsym Shift+XF86AudioRaiseVolume $exid pctl volume 0.05+

# Sreen brightness controls
bindsym XF86MonBrightnessUp $exid brightness up
bindsym XF86MonBrightnessDown $exid brightness down

# Media player controls
bindsym XF86AudioPlay $exid pctl play-pause
bindsym Shift+XF86AudioPlay $exid pctl NEXT
bindcode 164 $exid pctl play-pause

bindsym XF86AudioPause $exid pctl pause

bindsym XF86AudioNext $exid pctl next
bindsym button9 $exid pctl next

bindsym XF86AudioPrev $exid pctl previous
bindsym button8 $exid pctl previous

# Change Audio Settings
bindsym $super+Ctrl+Shift+o $exid audio_program_volume
bindsym $super+Shift+o $exid audio_program_sink
bindsym $super+Ctrl+o $exid audio_profile
bindsym $super+o $exid audio_sink

# Display Manager
bindsym $super+Shift+p $exid rofi-randr
bindsym $super+Ctrl+p $exid autorandr --change --force
#bindsym $super+p $exid /usr/bin/lxrandr
bindsym $super+b $exid bluetooth_dmenu

# Lock
bindsym $super+l $exid loginctl lock-session

# WiFi-Manager
bindsym $super+i $exid networkmanager_dmenu

# Mount-Manager
bindsym $super+u $exid udiskie-dmenu

bindsym $super+x exec xkill

# Clipboard-Manager
bindsym $super+Shift+v $exid sh -c 'CM_DIR=$XDG_CACHE_HOME/clipmenud clipmenu -i && xdotool key --clearmodifiers ctrl+shift+v'
bindsym $super+Ctrl+v $exid sh -c 'CM_DIR=$XDG_CACHE_HOME/clipmenud clipmenu -i'

# Screenshots
# Selection > Clipboard
bindsym Print --release $exid "scrot -s /tmp/screenshot-$(date +%FT%T.%2N).png -e 'xclip -selection clipboard -t image/png < $f'"
# Full Screen > Clipboard
bindsym Shift+Print $exid "scrot /tmp/screenshot-$(date +%FT%T.%2N).png -e 'xclip -selection clipboard -t image/png < $f'"
# Selection > File
bindsym Ctrl+Print --release $exid "scrot -s $HOME/Screenshots/$(date +%FT%T.%2N).png"
# Full Screen > File
bindsym Ctrl+Shift+Print $exid "scrot $HOME/Screenshots/$(date +%FT%T.%2N).png"

bindsym $super+c exec "exo-open --launch WebBrowser"
bindsym $super+Shift+c exec "exo-open --launch WebBrowser --incognito"
bindsym $super+g exec sh -c 'dmenu -p google | tr " " "+" | xargs -r -I{} xdg-open https://google.com/search\?q\={}'

for_window [class="jetbrains-idea" title="Debug"] floating disable
for_window [class="java-lang-Thread" instance="java-lang-Thread"] floating enable
for_window [class="jetbrains-.*" title="win0"] floating enable
for_window [class="jetbrains-.*" title="Welcome to IntelliJ IDEA"] floating enable

for_window [class="Kazam"] floating enable

#hide_edge_borders both
#new_window none

set $alt Mod1

bindsym $super+Page_Up workspace prev
bindsym $super+Page_Down workspace next

# switch to workspace
bindsym $super+1 workspace 1
assign [class="Thunderbird"] 1
for_window [class="Thunderbird"] move to workspace 1

bindsym $super+2 workspace 2
bindsym $super+3 workspace 3
bindsym $super+4 workspace 4
bindsym $super+5 workspace 5
bindsym $super+6 workspace 6
bindsym $super+7 workspace 7
bindsym $super+8 workspace 8
bindsym $super+9 workspace 9
bindsym $super+0 workspace 10
bindsym $super+$alt+1 workspace 11
bindsym $super+$alt+2 workspace 12
bindsym $super+$alt+3 workspace 13

bindsym $super+$alt+4 workspace 14:Discord
assign [class="discord"] 14:Discord
for_window [class="discord"] move to workspace 14:Discord

bindsym $super+$alt+5 workspace 15:Steam
assign [class="Steam"] 15:Steam
for_window [class="Steam"] move to workspace 15:Steam

bindsym $super+$alt+6 workspace 16:YouTube
assign [class="Google-chrome" title=".*YouTube$"] 16:YouTube
for_window [class="Google-chrome" title=".*YouTube$"] move to workspace 16:YouTube

bindsym $super+$alt+7 workspace 17:Netflix
assign [class="Google-chrome" title="Netflix"] 17:Netflix
for_window [class="Google-chrome" title="^Netflix$"] move to workspace 17:Netflix

bindsym $super+$alt+8 workspace 18:YMP
assign [class="youtube-music-desktop-app"] 18:YMP
for_window [class="youtube-music-desktop-app"] move to workspace 18:YMP

bindsym $super+$alt+9 workspace 19:Teams
assign [class="Microsoft Teams - Insiders"] 19:Teams
assign [class="Microsoft Teams"] 19:Teams
for_window [class="Microsoft Teams - Insiders"] move to workspace 19:Teams
for_window [class="Microsoft Teams"] move to workspace 19:Teams

bindsym $super+$alt+0 workspace 20:Chat
assign [class="Slack"] 20:Chat
for_window [class="Slack"] move to workspace 20:Chat
assign [class="Signal"] 20:Chat
for_window [class="Signal"] move to workspace 20:Chat
assign [class="TelegramDesktop"] 20:Chat
for_window [class="TelegramDesktop"] move to workspace 20:Chat
assign [class="whatsapp-nativefier-.*"] 20:Chat
for_window [class="whatsapp-nativefier-.*"] move to workspace 20:Chat

bindsym $super+Shift+u [window_role="UPDATES"] scratchpad show
for_window [window_role="UPDATES"] move scratchpad

# move focused container to workspace
bindsym $super+Shift+1 move container to workspace 1
bindsym $super+Shift+2 move container to workspace 2
bindsym $super+Shift+3 move container to workspace 3
bindsym $super+Shift+4 move container to workspace 4
bindsym $super+Shift+5 move container to workspace 5
bindsym $super+Shift+6 move container to workspace 6
bindsym $super+Shift+7 move container to workspace 7
bindsym $super+Shift+8 move container to workspace 8
bindsym $super+Shift+9 move container to workspace 9
bindsym $super+Shift+0 move container to workspace 10
bindsym $super+Shift+$alt+1 move container to workspace 11
bindsym $super+Shift+$alt+2 move container to workspace 12
bindsym $super+Shift+$alt+3 move container to workspace 13
bindsym $super+Shift+$alt+4 move container to workspace 14:Discord
bindsym $super+Shift+$alt+5 move container to workspace 15:Steam
bindsym $super+Shift+$alt+6 move container to workspace 16:YouTube
bindsym $super+Shift+$alt+7 move container to workspace 17:Netflix
bindsym $super+Shift+$alt+8 move container to workspace 18:YMP
bindsym $super+Shift+$alt+9 move container to workspace 19:Teams
bindsym $super+Shift+$alt+0 move container to workspace 20:Chat

# move workspace to output
bindsym Ctrl+$super+Right move workspace to output right
bindsym Ctrl+$super+Left move workspace to output left
bindsym Ctrl+$super+Up move workspace to output up
bindsym Ctrl+$super+Down move workspace to output down

set $external HDMI-1-1
set $external2 DP-1-1
set $external3 $external4 DP-2-1
set $external4 DP-1-1-1
set $internal eDP-1-1

workspace 1 output $external $external2 $external3 $external4
workspace 14:Discord output $internal
workspace 15:Steam output $external $external2 $external3 $external4
workspace 16:YouTube output $external $external2 $external3 $external4
workspace 17:Netflix output $external $external2 $external3 $external4
workspace 18:YMP output $internal
workspace 19:Teams output $internal
workspace 20:Chat output $internal

gaps outer 5
gaps inner 10
smart_borders on

exec --no-startup-id systemctl --user start i3-session.target
exec_always --no-startup-id systemctl --user start feh.service
