#!/usr/bin/env bash

set -ex

# To prevent accidental locking
sleep 1

if lsusb | grep -E '1050:(0407|0116)' &> /dev/null
then
  exit 0
fi

i3-msg -s $XDG_RUNTIME_DIR/i3/ipc-socket.* exec /usr/local/bin/custom/lock
