plugin:
  node-shell:
    shortCut: Ctrl-S
    confirm: false
    description: Open Shell in Node
    scopes:
      - nodes
    background: false
    command: kubectl
    args:
      - node-shell
      - --context
      - $CONTEXT
      - $COL-NAME
